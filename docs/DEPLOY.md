# 发布到 GitHub Pages

网站是纯静态页面，所有文件都位于仓库的 `docs/` 目录。

1. 将改动推送到 GitHub 的默认分支。
2. 打开仓库 **Settings → Pages**。
3. 在 **Build and deployment** 中选择 **Deploy from a branch**。
4. 分支选择默认分支，目录选择 `/docs`，然后保存。

GitHub Pages 会自动发布此目录，后续更新 `docs/` 并推送即可更新网站。
